/*E07
Apresente a query para listar o nome dos autores com nenhuma publicação. Apresentá-los em ordem crescente.*/

SELECT nome
FROM autor AS au
LEFT JOIN livro AS li
    ON au.codAutor = li.autor

GROUP BY au.codAutor
HAVING li.autor IS NULL
ORDER BY au.nome;
